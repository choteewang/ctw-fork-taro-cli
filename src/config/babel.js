module.exports = {
  sourceMap: true,
  presets: [
    // ctw Inject
    // 'env'
  ],
  plugins: [
    // 'transform-class-properties',
    'transform-decorators-legacy',
    'transform-object-rest-spread'
  ]
}
